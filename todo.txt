TODO LIST OF NEEDED FEATURES:

make pixel screen with the proper structures in place (use a layer system and priority on first layer) => done for now, refining and information top half to add later

make proper first display => the need for health bar is now aparent, this should be the next step => display has been adapted to have empty space on top

make input reading with updates on input => done but it can take a chain of inputs, accepted when the player presses enter (this could allow the player to predo their inputs)

make character struct => being worked on => now had position and health attributes

make basic attacks to dodge => made random vertical columns more variety will be added => 4 basic attacks are now in

make first gameplay loop => health and attacks are in

>>> seperate files and make a makefile for compilation


phase 2 : 

make heads up display => frame made => health bar made, stamina bar and text display still needs implem => text display is done

add attacking and passing turn => passing has been added, attacking can just be added with an extra if => add the 4 basic abilities : attack, dash, shield, heal => shield isn't done but the rest implemented

add stamina => done

add aiming => done

make enemies cycle in and out => when enemy dies make a new enemy appear => done but with no transition so it's a bit weird

make exp and health augmenting (base for upgrades) => implemented with no messaging on level up => has level up message now

add text display on right as logs (or just info) => X

do pretty mob name display and health => done

do the great seperation => it has been done, makefile is now working too

add basic playing instruction in README.md => basic controls are there, to be added as more features are added in => updated

make a better attack stocking system to handle attacks coverig each other => implemented

display where the enemy is (custom ascii arts?) => all enmies have the same sprite but they are now displayed and can move around

better transition in between battling monsters => now asks for if you want to continue or quit 

exp bar => done

monster leveling => done but maybe will need rebalancing one day

save file system => savefiles are implemented but without a confirmation menu

phase 3:

better onboarding and tutorial => first attempt of it done

better readme instructions => done

reminder for dashes and heals if they are not used ? => only heal reminder is in

more enemies and more attacks (design them) => some attacks and enemies and attacks were added => some ajusting has been done

make attack being added to the queue always go through a check with play area
